#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {

	// input n
	int n;
	cin >> n;

	// n에 대한 min 배열
	vector<int> min_arr;

	// index 0 과 index 1 에 대한 초기화 , vector_size = 2
	min_arr.push_back(0); // index 0
	min_arr.push_back(1); // index 1









	return 0;
}